# Copy to .env and edit as needed.
# docker-compose.yml picks this up automatically.

# Port the host exposes (container always uses 8000 internally)
PORT=8000

# Simulation tuning
STRATEGOS_DEFAULT_TIME_SCALE=1.0
STRATEGOS_CHECKPOINT_INTERVAL=1000
STRATEGOS_LOG_LEVEL=INFO
